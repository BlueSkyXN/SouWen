# Conformance Inputs

Owner: Contract Validation.

Language-neutral conformance inputs and expected results belong here. Suites must run
deterministically without importing or installing the `souwen` Python package unless a later SPEC
explicitly defines a language-binding conformance stage.
