# JSON Schema Sources

Owner: Canonical DTO contract.

Language-neutral, versioned JSON Schema sources belong here. Python and TypeScript models must not
silently replace these sources as the canonical contract.
